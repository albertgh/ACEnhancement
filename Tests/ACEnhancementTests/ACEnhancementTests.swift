import XCTest
@testable import ACEnhancement

final class ACEnhancementTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ACEnhancement().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
